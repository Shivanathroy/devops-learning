# Terraform Project
Deploys an Azure Resource Group using Terraform.

## Usage
```bash
terraform init
terraform plan -var-file="envs/dev.tfvars"
terraform apply -var-file="envs/dev.tfvars"
```
updated
# trigger ci
